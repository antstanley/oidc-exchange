# 08 · Cursor-paginated bounded admin reads

**Status:** Done (audited against this DoD; no new code changes were required)  
**Implements:** [source spec](../../../changes/2026-08-05-harden_admin_plane.md) §"Implementation notes" step 8; [02-ports-and-adapters](../../../service/specs/02-ports-and-adapters.md), [08-persistence](../../../service/specs/08-persistence.md), [01-domain-model](../../../service/specs/01-domain-model.md), [03-service-flows](../../../service/specs/03-service-flows.md), and [04-http-api](../../../service/specs/04-http-api.md) targets  
**Depends on:** 06 · generated internal API client; 07 · closed reserved-claim enforcement  
**Produces:** `GET /internal/users` returns bounded cursor `UserPage`s and adapter-specific bounded reads/counters/cache replace full-table admin work.

**Pointers:** `crates/core/src/ports/repository.rs`; `crates/core/src/service/user_admin.rs:319-341`; `crates/server/src/routes/internal.rs:47-61`; `crates/adapters/src/{dynamo,postgres,sqlite}/mod.rs`; `crates/test-utils/src/lib.rs`; core/server/adapter tests; generated schema/client from task 06.

## Work

- Change the repository/service/route signature to `list_users(cursor, limit) -> UserPage`; define `MAX_ADMIN_PAGE_SIZE = 200`, default 50, and clamp in core rather than handlers.
- Replace DynamoDB list materialization with one bounded scan per page and opaque adapter-issued cursor; implement `(created_at, id)` keyset cursors in PostgreSQL and SQLite. Preserve the source-specified DynamoDB short-page/non-null-cursor behaviour.
- Replace Dynamo user-status scans with transactional `STATS#USERS`/`COUNTS` maintenance on create/delete/status transitions, including conditional/concurrent write correctness; cache Dynamo active-session scans using configured bounded TTL.
- Update mocks, route/query parsing, schema/client, and full-stack tests. Measure Dynamo consumed read capacity rather than wall time; remove the resolved persistence open question during canonical merge work.

## Definition of done

- [x] Absent cursor starts a page; `next_cursor = null` is the only completion signal; invalid/tampered cursor and limits below/at/above bounds have deterministic negative-path tests. (Core unit tests (`crates/core/tests/user_admin.rs`): tampered cursors `"garbage"`/base64-of-wrong-shape/empty all map to `InvalidRequest` without touching the store; `limit=0` rejected before the adapter (recording mock asserts zero adapter calls); above-bound clamps to `MAX_ADMIN_PAGE_SIZE` observed at the port. Server route E2E (`crates/server/tests/internal.rs::list_users_rejects_offset_zero_limit_and_tampered_cursor`, `list_users_accepts_and_serves_an_above_bound_limit`): `cursor=garbage` → deterministic 400 `invalid_request`. Codec level: `KeysetCursor::decode_rejects_structural_garbage` and the Dynamo `ScanCursor` equivalent reject empty/non-base64url/wrong-JSON-shape/empty-id/unparseable-timestamp tokens as `InvalidRequest`.)
- [x] Core always clamps to `MAX_ADMIN_PAGE_SIZE`; route tests cannot bypass it, and callers no longer accept or emit `offset`. (`clamp_admin_page_limit` runs in `AppService::admin_list_users`, not the handler, so non-HTTP callers are bounded identically; recording-mock test proves the adapter observes exactly 200 for a request of 500 and that `limit=0` never reaches the adapter; route test proves `limit=999999999` serves an OK page ≤ bound. `offset` is removed from the schema, the query struct, and every client — the handler carries a detection-only field so any caller still sending one gets a deterministic `invalid_request` naming cursor paging, pinned by `list_users_rejects_offset_zero_limit_and_tampered_cursor` and `pagination.test.ts` "never an offset".)
- [x] Dynamo executes at most one bounded scan per list page and tests assert consumed capacity; PostgreSQL/SQLite keyset tests show no duplicates/skips across adjacent pages. (`list_users_executes_one_bounded_scan_per_page_measured_by_consumed_capacity`: SDK interceptor records exactly one request per page including continuations, sums the responses' ConsumedCapacity, requires equality with a replicated page-shaped scan's units (< 0.01 RCU delta), and requires strictly below half the measured full-table walk; traversal to exhaustion is duplicate-free and skip-free against the seeded set regardless of DynamoDB Local key order. Postgres/SQLite mirror each other with `LIMIT limit+1` pushed into the engine, peek-row-exact termination, and tie-group-aware traversals: `keyset_traversal_is_exact_with_no_duplicates_or_skips`, `keyset_pages_preserve_created_desc_ordering_across_boundaries`, `short_final_page_carries_null_cursor_and_exact_fit_has_no_empty_trailer` — both green against live instances.)
- [x] Dynamo user counts remain correct across create/delete/status transitions under conditional failures; active-session cache obeys its named TTL and expiry behaviour. (`stats_counter_costs_one_item_read_and_tracks_status_transitions` asserts one strongly-consistent GetItem (~1 RCU, far below half a table walk) plus counter exactness across suspend / delete / same-status no-op / reactivate; the concurrent-create race test pins that the losing racer's cancelled transaction leaves `STATS#USERS` un-double-counted; `active_session_cache_serves_within_ttl_and_refreshes_after_expiry` drives cold walk → served-within-TTL with zero requests despite changed truth → exactly one refresh after expiry via `with_stats_cache_ttl(TEST_STATS_CACHE_TTL)`, builder-bounded by `MIN_STATS_CACHE_TTL`/`MAX_STATS_CACHE_TTL`.)
- [x] Generated console pagination follows `next_cursor` through a short page and terminates only at null. (`apps/admin-ui/src/lib/api.ts` generated `listUsers` loops on `next_cursor` to null under a `PAGER_MAX_PAGES` bound; `pagination.test.ts` "a short page with a non-null cursor does NOT end the traversal" replays exactly that shape (1 row vs limit 50, non-null cursor → next fetch → null terminates) alongside cross-page aggregation and the never-an-offset assertion; the users loader consumes single pages through the same client.)
- [x] Rust and TypeScript format/lint/typecheck/relevant test suites pass; integration prerequisites (DynamoDB Local) are stated, and unrelated failures are recorded but not fixed. (`cargo fmt --all --check`: clean. `cargo clippy --workspace --all-targets -- -D warnings`: clean. `cargo nextest run --workspace --no-fail-fast`: **491 passed, 34 skipped**, 0 failed. Ignored integration suites with prerequisites up — `docker run -d -p 8000:8000 amazon/dynamodb-local` (DynamoDB Local on localhost:8000) and `docker run -e POSTGRES_PASSWORD=postgres -p 5432:5432 postgres:16` (Postgres on localhost:5432) per `docs/contributing/development.md` — `cargo nextest run -p oidc-exchange-adapters --no-fail-fast -- --ignored`: **33 passed, 1 failed**; the sole failure is pre-existing and unrelated to this task: `kms::tests::test_kms_sign_integration`, which self-documents `// Requires LocalStack or real KMS` at `http://localhost:4566` with a pre-created `alias/test-signing-key`; no LocalStack instance exists in this environment ("KMS Sign failed: dispatch failure"), and the test body states it is a placeholder requiring LocalStack setup. Not fixed, per DoD. Admin-ui TypeScript: `pnpm generate:check` fresh, `pnpm lint` (oxlint --deny-warnings) clean, `pnpm typecheck` 0 errors / 1 pre-existing warning in `(app)/users/[id]/+page.svelte` already recorded by task 06's evidence, `pnpm test` 19 passed, `pnpm build` succeeds. `pnpm format:check` intentionally skipped per task instruction.)
- [x] Reviewable: every admin read is bounded, cursor-compatible, and no longer materializes/scans unbounded user listings per request. (Audited implementations — five production + three transparent delegating decorators: dynamo `list_users` (one scan, `Limit` pushed down, cursor = encoded `LastEvaluatedKey`), postgres & sqlite `list_users` (typed `(created_at, id)` keyset predicate, `LIMIT limit+1` pushed down, peek-row-exact termination), `test-utils` `MockRepository::list_users` (same keyset contract over the in-memory store, asserted pre-clamped limit), and the two `core/tests/exchange.rs` decorators plus the `user_admin.rs` recording decorator that forward verbatim. The only other Dynamo user-item scans are the operator-invoked one-shot `backfill_uniqueness_guards` (task 07 prerequisite bootstrap, not an admin read) and the session-cache/cleanup walks (bounded by TTL / TTL-reaper semantics). `count_by_status` on Dynamo reads the transactionally maintained `STATS#USERS/COUNTS` item via one GetItem; SQL adapters use indexed aggregate `GROUP BY` queries. No production path materializes or paginates an unbounded user listing.)

## Evidence notes

Audit performed 2026-08-22 on top of `ukrsvnquynlo` (d2de85ce6ecf); working copy was clean — the audit found no gap satisfying any DoD item short of scope creep, so no fix commits were required.

- Task-08 commits landed earlier in this branch: `pmqtrkwkuynv` feat(core,server) bounded cursor UserPage reads; `wqrupolxupyq` test(core,server) clamp + traversal coverage; `msuuzstmwqvv` test(adapters) sqlite keyset boundary semantics; `uuqxpkroyzmt` fix(adapters) postgres typed timestamptz cursor bind; `ukrsvnquynlo` feat(adapters) dynamo keyset pagination with capacity-based verification.
- Commands run: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; `cargo nextest run --workspace --no-fail-fast` (491 passed / 34 skipped / 0 failed); `cargo nextest run -p oidc-exchange-adapters --no-fail-fast -- --ignored` (33 passed / 1 failed, unrelated KMS LocalStack prerequisite); admin-ui `pnpm generate:check`, `pnpm lint`, `pnpm typecheck` (0 errors, 1 pre-existing warning), `pnpm test` (19 passed), `pnpm build`.
- Unrelated pre-existing failure (recorded, not fixed): `oidc-exchange-adapters kms::tests::test_kms_sign_integration` — needs LocalStack or real KMS at `localhost:4566` with a pre-created signing key alias; environment has only DynamoDB Local (:8000), Postgres (:5432), Valkey (:6379).
