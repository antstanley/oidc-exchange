---
---

Adopt Changesets for release versioning and changelog generation (repo tooling; no
published-package changes). `pnpm changeset` records release impact; `pnpm changeset:version`
bumps every manifest (npm, Cargo, Python) and lockfile in lockstep and updates the docs
versions manifest.
