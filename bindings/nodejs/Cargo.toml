[package]
name = "oidc-exchange-nodejs"
version.workspace = true
edition.workspace = true

[features]
default = []
conformance = ["oidc-exchange-ffi/conformance"]

[lib]
crate-type = ["cdylib"]

[dependencies]
oidc-exchange-ffi = { path = "../../crates/ffi" }
napi = { version = "2", features = ["async", "napi9"] }
napi-derive = "2"

[build-dependencies]
napi-build = "2"
