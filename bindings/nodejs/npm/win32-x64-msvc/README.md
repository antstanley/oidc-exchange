# @oidc-exchange/win32-x64-msvc

The **Windows x64 (MSVC)** native binary for [`@oidc-exchange/node`](https://www.npmjs.com/package/@oidc-exchange/node).

It is installed automatically as an `optionalDependency` of `@oidc-exchange/node` on this platform. **Don't install it directly** — install [`@oidc-exchange/node`](https://www.npmjs.com/package/@oidc-exchange/node) instead.

MIT licensed. See the [main repository](https://github.com/antstanley/oidc-exchange).
