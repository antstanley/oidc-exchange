{
  "name": "oidc-exchange-fastify-example",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node --import tsx index.ts",
    "test": "node --import tsx --test ../body-limit.test.ts body-limit.test.ts",
    "typecheck": "tsc --noEmit --module nodenext --moduleResolution nodenext --target es2022 --lib es2022,dom --types node index.ts ../body-limit.ts --skipLibCheck --ignoreConfig",
    "lint": "tsc --noEmit --module nodenext --moduleResolution nodenext --target es2022 --lib es2022,dom --types node index.ts ../body-limit.ts --skipLibCheck --ignoreConfig"
  },
  "dependencies": {
    "@oidc-exchange/node": "^0.1.0",
    "fastify": "^5"
  },
  "devDependencies": {
    "tsx": "^4",
    "@types/node": "^24",
    "typescript": "^6"
  }
}
