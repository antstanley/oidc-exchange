[package]
name = "oidc-exchange-core"
version.workspace = true
edition.workspace = true

[dependencies]
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
async-trait.workspace = true
chrono.workspace = true
tracing.workspace = true
ulid.workspace = true
base64.workspace = true
sha2.workspace = true
rand.workspace = true
hex.workspace = true
toml.workspace = true
# Constant-time equality for `Secret<String>`; the server crate uses the same crate for
# its bearer-check comparison.
subtle = "2"

[dev-dependencies]
tokio.workspace = true
oidc-exchange-test-utils.workspace = true
base64.workspace = true
sha2.workspace = true
hex.workspace = true
serde_json.workspace = true
chrono.workspace = true
# Task 07 structural proof: UI compile-fail cases asserting that `Secret<T>` refuses
# every formatting and tracing capture path; expected rustc diagnostics are committed
# as `.stderr` fixtures beside each case (re-bless with TRYBUILD=overwrite).
trybuild = "1"
