[package]
name = "oidc-exchange-conformance"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
oidc-exchange-ffi = { path = "../crates/ffi" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tempfile = "3"
